# behat.yml
default:
    autoload:
        "": tests/bootstrap

    suites:
        default:
            paths:
                - tests/features
    formatters:
        progress: true
wip:
    suites:
        default:
            filters:
                tags: '@wip'
